name: caddy
services:
  caddy:
    image: caddy:latest
    volumes:
      - $PWD/Caddyfile:/etc/caddy/Caddyfile
      - $PWD/site:/srv
      - ./data:/data
      - ./config:/config
    ports:
      - '80:80'
      - '443:443'
      - '443:443/udp'
    restart: unless-stopped
